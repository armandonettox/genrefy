services:
  genrefy:
    build: .
    container_name: genrefy
    ports:
      - "8005:8501"
    env_file:
      - .env
    volumes:
      - spotify-cache:/app/cache
      - genrefy-config:/app/user_config
    restart: unless-stopped

volumes:
  spotify-cache:
  genrefy-config:
